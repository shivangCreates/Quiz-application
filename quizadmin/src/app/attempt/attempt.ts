import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-attempt',
  imports: [RouterLink],
  templateUrl: './attempt.html',
  styleUrl: './attempt.css',
})
export class Attempt {

}
